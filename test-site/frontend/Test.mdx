import Counter from './Counter.jsx';

# 🏝️ MDX in Rust-Tropical

This page is authored entirely inside an **MDX file**! The Markdown code parses cleanly on the server into valid elements.

Below is an interactive island sitting directly inside the markdown text wrapper:

<Counter initial_count={100} />

### Benefits of this setup:
- Full Markdown writing speed
- Embed reactive Preact components inline
- Server-side pre-rendered content for fast SEO delivery
